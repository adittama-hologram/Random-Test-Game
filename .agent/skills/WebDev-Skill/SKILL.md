---
name: WebDev Skill
description: Helps with Web Development task, tools that needed to be used, and also the project structure that needed to be followed.
---

# WebDev Skill
Use Express js and three js (if required) to develop a web

# Docker
Make sure to use docker container

# Project Structure
Use these path when developing a web
/app
├── config/
├── database/                 
├── routes/
├── models/
├── views/
/public
├── dist/
├── images/
/src
/node_modules
/server.js
/package.json
/.env

# 404 Page
If route is not found create a 404 page and redirect to the 404 page (make sure inside that page include text that say "Masi Proses Boss").

# Database (if required)
Use MongoDB for database.

# API (if required)
Use REST API for API.

# Assets 
If Assets such as images, icons, etc is required, please use the CC0 assets.

# Gitignore
When developing web app, make sure to add .gitignore file that typically used in projects that use express js

